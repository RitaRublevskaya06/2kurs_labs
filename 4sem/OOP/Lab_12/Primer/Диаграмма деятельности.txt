@startuml
!theme plain
start
:Ввод данных (имя, фамилия, email, логин, пароль);
if (Валидность email?) then (Да)
  if (Логин уникален?) then (Да)
    :Сохранение в БД;
    :Успешная регистрация;
    stop
  else (Нет)
    :Вывод ошибки: "Логин занят";
  endif
else (Нет)
  :Вывод ошибки: "Некорректный email";
endif
:Возврат к вводу данных;
stop
@enduml